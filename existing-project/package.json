{
  "name": "existing-project",
  "version": "0.1.0",
  "description": "existing-project project",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}