#!/usr/bin/env sh

# Run server linting
npm run lint:server

# TODO: Fix root directory test execution - temporarily run directly in server directory
# Run server unit tests with coverage enforcement directly in server directory
cd server && npm run test:coverage