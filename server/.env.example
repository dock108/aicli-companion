# Claude Companion Server Configuration

# Server settings
PORT=3001
HOST=0.0.0.0

# Authentication (recommended for security)
# Generate a secure token: openssl rand -base64 32
AUTH_TOKEN=

# CORS settings (comma-separated list of allowed origins)
ALLOWED_ORIGINS=*

# Feature toggles
ENABLE_BONJOUR=true

# Logging
NODE_ENV=development